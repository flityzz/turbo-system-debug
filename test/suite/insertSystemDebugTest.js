const assert = require('assert');
const vscode = require('vscode');
const { insertSystemDebug } = require('../../helper');

const insertSystemDebugSuite = () => {
    
}

module.exports ={
    insertSystemDebugSuite
}